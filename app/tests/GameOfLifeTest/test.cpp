#include "pch.h"
#include "../../source/core/engine/Engine.h"
using namespace gol::app::core::engine;

TEST(TestCaseName, TestName) {

 //Engine engine = Engine(1,1);
 // EXPECT_EQ(1, 1);
 // EXPECT_TRUE(true);
}